<div class="contents row">
  <% @posts.each do |post| %>
    <div class="content_post" style="background-image: url(<%= post.image %>);">
      <p><%= post.title %></p>
      <span class="name">
        <%= post.author %>
      </span>
    </div>
  <% end %>
</div>